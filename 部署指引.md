# 智析阅卷 · 试卷分析系统 — 部署上线指引

本系统是**纯前端应用**（`index.html` + `style.css` + `app.js`），无需服务器、无需数据库，因此可以免费部署到任意静态托管平台，几分钟后即可获得一个**公网链接**，把链接发给老师和学生即可在电脑、手机上访问使用。

## 部署前须知（两个重点，务必先看）

1. **数据保存在浏览器本地**：老师录入的模板、考试、成绩都保存在自己浏览器的 localStorage 中。也就是说：
   - 换浏览器/换设备，数据不会自动同步；
   - 老师在同一台设备、同一浏览器上使用，数据一直都在；
   - **强烈建议老师用完在右上角"备份数据"导出备份 Excel**；换设备时用"恢复数据"导入即可。
2. **协议需为 `https://`**：部分浏览器只在安全上下文（https 或 localhost）下开放某些能力。用下方任一托管平台得到的正式链接都是 https，没问题。

---

## 方式一：Netlify（最推荐，拖拽即可，5 分钟）

1. 打开官网并登录：https://app.netlify.com/drop （可用 GitHub 账号直接登录）。
2. 把项目的这几个文件拖进页面中间的虚线框：
   - `index.html`
   - `style.css`
   - `app.js`
3. 松开鼠标后，Netlify 会自动上传并发布，页面会跳出一个类似 `https://xxxxx.netlify.app` 的地址，这就是你的公网链接。
4. 把这个链接发给老师/学生，他们打开即可使用。
5. 以后想改名：登录后在 Site settings → Change site name 里改。

> 每次想更新，直接把新版文件再拖进去覆盖即可。

---

## 方式二：Vercel（命令行，稍快更灵活）

```
npm i -g vercel
vercel
```
在项目根目录（含 index.html 的目录）执行，按提示默认即可，最后会得到一个 `https://xxx.vercel.app` 链接。

---

## 方式三：GitHub Pages（适合会 Git 的用户）

1. 在 GitHub 新建一个仓库，把三个文件传上去。
2. 打开仓库 Settings → Pages → Source 选 `main` 分支 → Save。
3. 稍等 1～2 分钟，即可通过 `https://你的用户名.github.io/仓库名/` 访问。

---

## 本地预览（部署前想先看看效果）

在含 `index.html` 的目录执行：
```
python -m http.server 8123
```
浏览器打开 http://127.0.0.1:8123/ 即可。

---

## 日常使用流程（老师视角）

1. **试题模板（推荐智能识别）**：在"试题模板"页点"✨智能识别导入"，上传试卷（及可选答案）PDF/Word，系统自动识别题号、分值、题型生成草稿，你快速核对补一两处后保存。也可手动逐题录入。一次建好可复用于多场考试。
   - 提示：**文字版 PDF（如 Word 另存的 PDF）识别率最高**；扫描/拍照版会启用 OCR，较慢且模糊件可能识别不准，用后务必核对题号与分值。
2. **考试管理**：创建考试 → 选择科目与模板 → 上传试卷/参考答案（PDF 或 Word，系统内可在线预览，仅归档备份）。
3. **成绩录入**：从阅卷系统导出的 Excel（行=学生、列=小题得分）一键导入；也可以直接在线填表。
4. **生成报告**：点击"生成分析报告"，系统按规则引擎自动给出逐题评价与整体评价。
5. **下载结果**：
   - 《班级整体分析.xlsx》：全班逐题得分率、失分率、难度、薄弱学生名单、分数段分布。
   - 《学生个人评价报告.xlsx》：目录页 + 每位学生一个工作表，含逐题评价、优势/薄弱点、整体评语、复习建议。

## 常见问题

- **Q：我换了电脑，之前的数据不见了？** A：网页数据存在浏览器本地。请在原设备右上角"备份数据"导出 Excel，再在新设备"恢复数据"导入。
- **Q：可以在多个老师直接共享同一份数据吗？** A：当前为纯前端、无后端，不提供账号同步。如果未来需要多端同步/账号体系，需要加后端与数据库（可另行开发）。
- **Q：PDF 预览只显示前几页？** 为性能考虑默认缓存前 8 页用于预览，完整内容建议直接下载原文件查看。